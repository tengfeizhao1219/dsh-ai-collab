# 协作总规（COLLABORATION）

> 本项目协作机制总纲。所有角色必读。由 O 主控维护。
> 由 ai-collab 插件生成，机制版本：{{MECH_VERSION}}，初始化日期：{{START_DATE}}。

> 核心理念：**文件即通信**——所有角色通过 git 仓库 + 约定文件交换信息，不靠会议、不靠催进度。

---

## 一、团队角色映射

本项目采用「**O 主控 + 专业角色**」模型（详细身份见 ROLE_CARDS.md）：

| 角色 | 身份 | 职责 |
| **O 主控** Orchestrator | 项目总控 + 编排 | 拆解任务、派发子 Agent、关口检查、最终决策 |
| **I 基础设施** Infra | 技术负责人 | 项目骨架、数据模型、接口契约、部署 |
| **A 适配/接入** Adapter ×N | 外部接入 | 差异化接入（API/RSS/爬取/第三方对接） |
| **P 处理/核心** Process | 核心逻辑 | 核心流程、业务规则、数据变换 |
| **D 交付/前端** Delivery | 前端/UI 全栈 | 页面、UI 规范、1:1 原型、UI/UX 验收 |
| **Q 校验** QA | 质量把关 | 交叉校验、一致性、红线审计 |
| **K 文档** Knowledge | 文档与知识 | 需求/调研/设计/导航索引/机制文档 |

> **执行模型**：O 主控持有全局上下文（一个主会话/主代理）；需要并行时，用**子 Agent 临时扮演** I/A/P/D/Q/K。子 Agent 启动时读取 ROLE_CARDS.md + 约定文件获得身份；交付物写回约定文件，由 O 汇总后做关口检查。

---

## 二、核心约定文件（角色必读）

| 文件 | 作用 | 维护者 |
| `CONTEXT.md` | 项目速览（任何角色的「记忆外挂」，第一必读） | 全员更新 |
| `ROLE_CARDS.md` | 角色卡（身份/可以/不可以/产出目录/阶段/上级） | O |
| `TASK_BOARD.md` | 统一任务看板：认领、状态、优先级、依赖 | 全员认领 |
| `COMMLOG.md` | 沟通交接记录（倒序看最新） | 全员 |
| `RELAY.md` | 既定任务跟踪表（计划内流水线，不阻塞） | O |
| `COLLABORATION.md`（本文件） | 协作总规 | O |
| `NAVIGATION.md` | 文档导航与交叉引用索引（AI 读者入口） | K |
| `LEARNINGS.md` | 教训库：踩坑记录（症状/根因/正确做法），同一坑不踩第二次 | 修复者本人 |
| `ADR.md` | 决策日志：owner 拍板与关键技术决策的结构化存档 | O |

**文档目录约定**（按阶段分目录，避免互相覆盖）：

```plain text
docs/
├── 01-需求规划/      ← K / O 读写
├── 02-产品设计/      ← D / O 读写（UI 规范、demo）
├── 03-技术方案/      ← I / A / P 读写（架构、复用审计）
├── 04-开发实现/      ← I / A / P / D 读写（代码、交接单）
├── 05-测试验收/      ← Q / O 读写
└── 06-上线复盘/      ← O 读写
```

---

## 三、Git 纪律（全员直连）

1. **改前先拉**：`git pull --rebase`。
2. **改后立即提交**：一次逻辑单元一次提交，commit message 写明改了啥。
3. **小步快推**：不在本地堆积大量改动。
4. **双端并行纪律**：认领同一任务前先看 TASK_BOARD 状态，避免双写冲突；关键决策写 COMMLOG。
5. **检查点快照**：每阶段关口通过时打 git tag（`git tag phase-1-done && git push --tags`）；阶段内失败需回退时，用 `git checkout <tag>` 从最近快照恢复，而不是重跑（对齐 LangGraph 检查点恢复）。

---

## 四、任务流转（认领 → 交付 → 下一棒）

1. **认领前先核对归属**：看 RELAY.md 确认当前阶段任务是否属于自己 → 去 TASK_BOARD.md 认领。
2. **状态机**：📋 待认领 → 🔄 进行中（填认领人）→ ✅ 完成（为下一角色建/激活任务）→ 🚫 阻塞（+原因）；失败/中断 → 🔁 重试中（附上次失败原因）或 ⛔ 放弃（附理由）。
3. **交付交接包**（完成交接类任务时填）：做了什么 / 产出物清单 / 给下一棒的提醒。

3. **影响面检查清单**（改动涉及数据/显示/部署/兼容任一层面时，交付前必填）：数据层影响 / 显示层影响 / 部署与生效验证（部署后必须验证生效，不能只信"部署成功"）/ 兼容性与回退。
4. **3 分支意图识别**（收到指令时先判）：
   - **主线**：「接棒/继续/该你」且 RELAY 显示当前阶段属于我 → 认领 🔴 任务。
   - **误用棒**：说接棒但 RELAY 显示不是我 → ① 提示正确角色 ② 立即转即席（两步不可省）。
   - **即席/直连**：对其他角色直接提诉求（问答、小改、临时需求）→ 权限内处理、轻量交付、不碰跟踪表；纯问答直接答。

---

## 五、关口检查（Gate Check）

阶段级任务全 ✅ 后，由 **O（主控）** 做关口检查：通过才开放下一阶段任务；不通过则回退并标记 🚫 + 阻塞项说明。关口记录追加到 TASK_BOARD.md。

**质量门禁（关口检查前置，硬性）**：
1. 先跑 `python3 scripts/check.py` 做约定文件一致性审计（退出码 0 才可过关口）。
2. 有自动化测试/lint/类型检查的项目：关口前必须跑通（`npm test` / `npm run lint` 等），结果写进关口记录。
3. 涉及部署的改动：交付包必须含「生效验证」证据（日志/截图/接口实测），不接受"部署成功"作为完成。

**DoD（完成定义，可脚本验证）**：任务标 ✅ 前逐条核对——
- [ ] 文档已更新（涉及文档/接口/配置的改动，同步更新对应约定文件与 NAVIGATION）
- [ ] 无 TODO/FIXME/占位残留（check.py 覆盖占位符项）
- [ ] 测试/校验通过（有测试跑测试；无测试跑 check.py + 语法检查）
- [ ] 影响面已声明（数据/显示/部署/兼容）且部署类改动含生效证据
- [ ] COMMLOG 已留痕（交接点）

---

## 六、owner 参与规则

- 用户（owner）仅在**关键决策点拍板**、在需凭证处（token/密钥/账号/资源）给料。
- AI **显式请求**资源，绝不自行假设或编造。
- 依赖未到位 → 标记「阻塞-待 owner 提供」，继续推进不依赖项，不空等。
- 所有产物（文档/代码/测试/配置）都以「AI 实现/输出」为前提生产，且必须是 **AI 可直接消费** 的形态：结构化、术语一致、带交叉引用、可独立验证。

---

## 七、验收闭环（专业领域，以 UI/UX 为例）

1. **设计交付物标准**（D 产出）：① 1:1 可交互原型 ② 精确规格文档 ③ 图标/交互规范。
2. **验收走查闭环**：D 角色做 UI/UX 验收；Bug 提单格式 = 现象/复现路径/期望/截图。
3. **不可自行推断**（其他角色）：❌ 不得自行改设计值（色值/透明度/圆角/间距/字重）；❌ 不得自行替换图标；❌ 不得省略状态覆盖（loading/empty/error）。✅ 遇技术限制先 @O 确认替代方案。
4. **硬约束**：写入 CONTEXT.md 硬前提；增量改动必须显式标记（如 `BRIDGE` 注释）且可整体摘除；Q 角色做红线审计。

---

## 八、同步通道（可选但推荐）

- **代码实时同步 Git**：`scripts/git_sync.sh` 守护每 60s 自动 commit+push。
- **文档实时同步 Notion**：`scripts/notion_sync.py` 守护每 60s 幂等同步（可选，需 token）。
- **凭证**：集中存于 secrets 文件（权限 600，不入库）。

---

## 九、会话继承（新会话启动必做）

新会话启动时，把 SESSION_BOOT.md 的提示词发给 AI，按序继承后再派发任务。
**关键认知：AI 云端记忆只带「你是谁」，文件机制才带「项目进行到哪」。**


---

## 十、教训库（LEARNINGS）

- **规则**：任何修复/返工（≥2 次尝试或 ≥1 小时）完成后，**当次必须**在 `LEARNINGS.md` 追加一条（症状/根因/正确做法）。
- **必读时机**：新阶段/新模块开工前，相关角色必读 LEARNINGS.md；O 派发任务时可提示对应教训。
- **目的**：同一个坑绝不踩第二次；知识从对话沉淀到文件。

## 十一、决策日志（ADR）

- **规则**：owner 拍板（技术栈/分支策略/范围/资源取舍）→ O **当次**写入 `ADR.md`（日期/决策/理由/备选/影响）。
- **目的**：决策可追溯；后续会话不用靠翻 COMMLOG 猜"为什么当时这么定"。

## 十二、归档与记忆卫生（防上下文膨胀）

- **COMMLOG 压缩轮转**：超过 30 条或跨月时，把旧条目压成「决策摘要」归档到 `docs/06-上线复盘/commlog-<月份>.md`（每条保留：日期/决策结论/涉及角色），主 COMMLOG 只留存活信息。
- **CONTEXT.md 分层**：主文件只放「当前状态」与高频不变规则；细节（技术细节/历史）拆到 `docs/` 子文件，新会话按需加载（@import 式渐进披露）。
- **ROLE_CARDS.md**：主文件控制在几十行（身份/可以/不可以），详细权限细则拆 `docs/03-技术方案/` 子文件。
- **目标**：新会话读五件套的 token 成本保持稳定，不随项目线性增长。


---

## 十三、复盘与持续优化（REVIEW · 让 AI 越用越强）

**目的**：不止于记录教训——定期从任务和对话中复盘，提炼效率瓶颈与 AI 能力短板，反哺优化角色卡/机制/工具/DoD，后续任务一轮比一轮高效。

**触发时机**（任一即可，O 负责组织）：
1. **关口检查时**（阶段级强制：先 `python3 scripts/review.py` 产出复盘输入，再写 REVIEW.md，最后过关口）
2. **批量完成**：每完成 5 个任务
3. **模式触发**：同一类返工出现 2 次 / 某区域连续 2 次修复 / 任务 🔁 重试
4. **定期**：每周或每阶段末

**复盘条目结构**（追加到 REVIEW.md，倒序）：做得好的（保持）→ 踩的坑（→ LEARNINGS）→ 效率瓶颈（review.py 数据）→ AI 能力短板 → 机制缺陷 → **优化行动（每条带去向，完成打勾）**。

**反哺闭环（复盘不落地 = 没复盘）**：
- 教训 → `LEARNINGS.md`（修复者记录）
- 决策 → `ADR.md`（O 记录）
- 角色短板 → 更新 `ROLE_CARDS.md`（收紧"不可以"/调整职责边界）
- 机制卡点 → 更新 `COLLABORATION.md` / `check.py` 审计规则 / DoD 验收项
- 通用经验 → **沉淀回 ai-collab 插件模板**（升级插件版本，新项目直接受益）——这就是"不断优化后续项目 AI 能力"的跨项目闭环

**职责**：O 组织复盘并追踪优化行动；所有角色在复盘时提供输入（自己任务的效率观察）。
